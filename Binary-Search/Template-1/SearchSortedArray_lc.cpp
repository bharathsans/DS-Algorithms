#include <bits/stdc++.h>
using namespace std;


class Solution {
public:
    int search(vector<int>& nums, int target) {
        
    }
};


int main(){

    Solution solve;

    vector<int> nums;
    nums.push_back(5);
    nums.push_back(6);
    for(int i = 0; i < 3 ; i++)
        nums.push_back(i);

    int target = 5;

    cout << solve.search(nums, target) << endl;
    
}